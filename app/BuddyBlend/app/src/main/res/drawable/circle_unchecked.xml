<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <stroke
        android:width="2dp"
        android:color="#757575" /> <!-- Màu viền khi unchecked -->
    <solid android:color="@color/transparent" /> <!-- Màu nền -->
    <size
        android:width="20dp"
        android:height="20dp" />
</shape>